<%= form_for(tpersona) do |f| %>
  <% if tpersona.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tpersona.errors.count, "error") %> prohibited this tpersona from being saved:</h2>

      <ul>
      <% tpersona.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :tipo %>
    <%= f.text_field :tipo %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
